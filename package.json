{
  "name": "love-letter-inspired",
  "version": "0.1.0",
  "description": "A realtime browser card game inspired by deduction letter games.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "check": "node --check app.js && node --check public/client.js && node --check public/service-worker.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@auth/express": "^0.11.0",
    "@auth/prisma-adapter": "^2.7.0",
    "@prisma/client": "6.19.0",
    "express": "^4.19.2",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "prisma": "6.19.0"
  },
  "engines": {
    "node": ">=18"
  }
}
